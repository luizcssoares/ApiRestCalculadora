apiVersion: v1
kind: Service
metadata:
  name: {{ include "apirestcalculadora.fullname" . }}
  labels:
    {{- include "apirestcalculadora.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "apirestcalculadora.selectorLabels" . | nindent 4 }}
